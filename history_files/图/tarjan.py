########################################################################
########################################################################
#
"""
tarjan 无向图求割点与桥



- dfn[i] dfs时间序
- low 返祖边指向的
"""

def tarjan_cut_vertces(n, edges):  # 输入n节点的无向图
    g = [[] for _ in range(n)]
    for a, b in edges:
        g[a].append(b)
        g[b].append(a)

    # dfn[i] dfs到节点i的时间
    # low[i] 定义为以下两种情况的最小值
    #

